version: "2"
sql:
  - engine: "postgresql"
    queries: "queries"
    schema: "../supabase/migrations"
    gen:
      go:
        package: "repository"
        out: "repository"
        sql_package: "pgx/v5"
overrides:
  go:
    rename:
      app_budget: Budget
      app_entry: Entry
      app_user: User
